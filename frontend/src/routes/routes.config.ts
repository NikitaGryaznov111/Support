export const ROUTES = {
  root: '/',
  user: '/:userId',
  tasks: 'tasks',
  task: '/:userId/tasks/:taskId',
  projects: 'projects',
  project: '/:userId/projects/:projectId',
  editTask: '/:userId/tasks/:taskId/edit',
  editTaskInProject: '/:userId/projects/:projectId/:taskId',
  taskInProject: '/:userId/projects/:projectId/:taskId',
  editProject: '/:userId/projects/:projectId/edit',
  error: '*',
  login: '/login',
  registration: '/registration',
} as const;
